name: GitHub Actions Project
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: echo "repo fetched"
      - run: ls -a

      - name: Build image
        run: echo "build image multistage"

      - name: create 2 tagged images

      - name: run image
        run: docker run "--name project java_project:latest"

      - name: login to docker hub
        run: "connect to docker with credentials"

      - name: upload image
        run: ""

