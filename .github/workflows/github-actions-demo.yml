name: GitHub Actions Project
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
        
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - run: echo "repo fetched"
      - run: ls -a
  
#      - name: Bump Version
#        run: mvn versions:set -DnewVersion=1.0.${{github.run_number}}
        

      - name: Build image
        run: docker build -t mavenproj .

#      - name: create 2 tagged images

#      - name: run image
#        run: docker run "--name project java_project:latest"

#      - name: login to docker hub
#        run: echo "connect to docker with credentials"

#      - name: upload image
#        run: echo ""

